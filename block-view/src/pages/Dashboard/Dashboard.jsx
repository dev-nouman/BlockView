import React from 'react'

const Dashboard = () => {
  return (
    <div>
      <h1>Dashboard</h1>
      <div>
        lets make dashboard page now... it should be a grid layout two cards first
        (total holdings)current value: $amount and a small change % , total investment: $amount

        below 2 cards square: portfolio allocation(pie chart of the coins we hold place holder for now), another card with 2 tabs top gainers top losers of our portfolio..

        then at the bottom: a table portfolio details: rank,name price total investment coins purchased, current value, profit/loss. two buttons above the portfolio which says export as pdf and csv.
      </div>
    </div>
  )
}

export default Dashboard
